// our-domain.com/new-meetup

import NewMeetup from "../../components/newMeetup";

// alternatively you could create new-meetup.js file in pages folder and it would be the same
const NewMeetUp = () => {
 return <><NewMeetup /></>
};

export default NewMeetUp;
